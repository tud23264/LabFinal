<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context=".MainActivity">
    <item android:id="@+id/action_delete" android:title="@string/text_delete"
        android:icon="@drawable/ic_action_delete" app:showAsAction="always"/>
    <item android:id="@+id/action_cancel" android:title="@string/text_cancel"
        android:icon="@drawable/ic_action_cancel" app:showAsAction="always"
        android:visible="false"/>
    <item android:id="@+id/action_confirm" android:title="@string/text_delete"
        android:icon="@drawable/ic_action_confirm" app:showAsAction="always"
        android:visible="false"/>
</menu>